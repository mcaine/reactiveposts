spring:
  application:
    name: reactiveposts
  cloud:
    kubernetes:
      loadbalancer:
        enabled: false
      enabled: false
      discovery:
        enabled: false
      reload:
        enabled: true
