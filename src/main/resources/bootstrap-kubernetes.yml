spring:
  application:
    name: reactiveposts
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: event
        monitoring-config-maps: true
      config:
        sources:
          - namespace: default
          - name: ${spring.application.name}